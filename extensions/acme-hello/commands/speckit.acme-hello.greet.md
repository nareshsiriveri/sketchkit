---
description: Greet the user and confirm the ACME Hello extension is installed and working.
---

# /speckit.acme-hello.greet

You are running the **ACME Hello** Spec-Kit extension.

When the user invokes this command:

1. Greet the user by name if known, otherwise greet them generally.
2. Confirm that the `acme-hello` extension is installed and active.
3. Briefly list what this extension can do (this is a template — replace with
   your organization's real command behavior).

Keep the response short and friendly.
